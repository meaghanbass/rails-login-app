<% 
=begin %>
<section class="jumbotron bg-white border border-gray-400 text-center">
  <h1>Rails Auth0 App</h1>
  <br>
  <%= button_to 'Login', 'auth/auth0', method: :post, class: 'bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-4 px-8 rounded inline-flex items-center', id: 'qsLoginBtn' %>
  <h3>Landing Page (logged out)</h3>
</section>
  <% 
=end %>

<% content_for :welcome do %>
  <h3 class="text-2xl">Landing Page (logged out)</h3>
<% end %>